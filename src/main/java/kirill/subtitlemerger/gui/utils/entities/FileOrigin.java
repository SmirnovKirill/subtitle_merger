package kirill.subtitlemerger.gui.utils.entities;

/**
 * In our application user can either select the file with the file chooser or enter the path to the file manually
 * through the text field.
 */
public enum FileOrigin {
    TEXT_FIELD,
    FILE_CHOOSER
}
