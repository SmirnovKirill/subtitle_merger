package kirill.subtitlemerger.gui.utils.entities;

/**
 * In our application the user can select a file either with the file chooser or enter the path to the file manually
 * through the text field.
 */
public enum FileOrigin {
    TEXT_FIELD,
    FILE_CHOOSER
}
